; this defines the assembly (written in dasm (Derek's assembly) for standard functions, will contain _start, and similar)
; I think this is where I would also want to write how function calls end up on the stack?
; All my c files will pretend include this, and "actually" include the headers so I get nice syntax for them
; I'll implement stdin/out here

_start:
	initialize other register to 0
	intitialize all interrupts to 0
    ; setup registers from binary
    
    run main;
    put value somewhere
    MOV STATUS_REG, terminate_program;
